function loadAnimDict(dict)
	while (not HasAnimDictLoaded(dict)) do
		RequestAnimDict(dict)
		Citizen.Wait(5)
	end
end

RegisterNetEvent("srp-hospital:items:gauze")
AddEventHandler("srp-hospital:items:gauze", function(item)
    loadAnimDict("missheistdockssetup1clipboard@idle_a")
    TaskPlayAnim( PlayerPedId(), "missheistdockssetup1clipboard@idle_a", "idle_a", 3.0, 1.0, -1, 49, 0, 0, 0, 0 ) 
    exports["srp_taskbar"]:StartDelayedFunction('Packing Wounds', 1500, function()
        TriggerEvent('srp-hospital:client:FieldTreatBleed')
        ClearPedTasks(PlayerPedId())
    end)
end)

RegisterNetEvent("srp-hospital:items:bandage")
AddEventHandler("srp-hospital:items:bandage", function(item)
    loadAnimDict("missheistdockssetup1clipboard@idle_a")
    TaskPlayAnim( PlayerPedId(), "missheistdockssetup1clipboard@idle_a", "idle_a", 3.0, 1.0, -1, 49, 0, 0, 0, 0 ) 
    exports["srp_taskbar"]:StartDelayedFunction('Using Bandage', 5000, function()
        local maxHealth = GetEntityMaxHealth(PlayerPedId())
		local health = GetEntityHealth(PlayerPedId())
		local newHealth = math.min(maxHealth, math.floor(health + maxHealth / 16))
        SetEntityHealth(PlayerPedId(), newHealth)
        TriggerEvent('srp-hospital:client:RemoveBleed')
        ClearPedTasks(PlayerPedId())
    end)
end)

RegisterNetEvent("srp-hospital:items:firstaid")
AddEventHandler("srp-hospital:items:firstaid", function(item)
    loadAnimDict("missheistdockssetup1clipboard@idle_a")
    TaskPlayAnim( PlayerPedId(), "missheistdockssetup1clipboard@idle_a", "idle_a", 3.0, 1.0, -1, 49, 0, 0, 0, 0 ) 
    exports["srp_taskbar"]:StartDelayedFunction('Using First Aid', 10000, function()
        local maxHealth = GetEntityMaxHealth(PlayerPedId())
		local health = GetEntityHealth(PlayerPedId())
		local newHealth = math.min(maxHealth, math.floor(health + maxHealth / 8))
        SetEntityHealth(PlayerPedId(), newHealth)
        TriggerEvent('srp-hospital:client:RemoveBleed')
        ClearPedTasks(PlayerPedId())
    end)
end)

RegisterNetEvent("srp-hospital:items:medkit")
AddEventHandler("srp-hospital:items:medkit", function(item)
    loadAnimDict("missheistdockssetup1clipboard@idle_a")
    TaskPlayAnim( PlayerPedId(), "missheistdockssetup1clipboard@idle_a", "idle_a", 3.0, 1.0, -1, 49, 0, 0, 0, 0 ) 
    exports["srp_taskbar"]:StartDelayedFunction('Using Medkit', 20000, function()
        SetEntityHealth(PlayerPedId(), GetEntityMaxHealth(PlayerPedId()))
        TriggerEvent('srp-hospital:client:FieldTreatLimbs')
        ClearPedTasks(PlayerPedId())
    end)
end)

RegisterNetEvent("srp-hospital:items:vicodin")
AddEventHandler("srp-hospital:items:vicodin", function(item)
    loadAnimDict("mp_suicide")
    TaskPlayAnim( PlayerPedId(), "mp_suicide", "pill", 3.0, 1.0, -1, 49, 0, 0, 0, 0 ) 
    exports["srp_taskbar"]:StartDelayedFunction('Taking Oxycodone', 1000, function()
        TriggerEvent('srp-hospital:client:UsePainKiller', 1)
        ClearPedTasks(PlayerPedId())
    end)
end)

RegisterNetEvent("srp-hospital:items:ifak")
AddEventHandler("srp-hospital:items:ifak", function(item)
    loadAnimDict("mp_suicide")
    TaskPlayAnim( PlayerPedId(), "amb@world_human_clipboard@male@idle_a", "idle_c", 3.0, 1.0, 10000, 49, 0, 0, 0, 0 ) 
    exports["srp_taskbar"]:StartDelayedFunction('Using IFAK', 10000, function()
        TriggerEvent('srp-hospital:client:UsePainKiller', 1)
        ClearPedTasks(PlayerPedId())
    end)
end)

RegisterNetEvent("srp-hospital:items:hydrocodone")
AddEventHandler("srp-hospital:items:hydrocodone", function(item)
    loadAnimDict("mp_suicide")
    TaskPlayAnim( PlayerPedId(), "mp_suicide", "pill", 3.0, 1.0, -1, 49, 0, 0, 0, 0 ) 
    exports["srp_taskbar"]:StartDelayedFunction('Taking Hydrocodone', 1000, function()
        TriggerEvent('srp-hospital:client:UsePainKiller', 2)
        ClearPedTasks(PlayerPedId())
    end)
end)

RegisterNetEvent("srp-hospital:items:morphine")
AddEventHandler("srp-hospital:items:morphine", function(item)
    loadAnimDict("mp_suicide")
    TaskPlayAnim( PlayerPedId(), "mp_suicide", "pill", 3.0, 1.0, -1, 49, 0, 0, 0, 0 ) 
    exports["srp_taskbar"]:StartDelayedFunction('Taking Morphine', 2000, function()
        TriggerEvent('srp-hospital:client:UsePainKiller', 6)
        ClearPedTasks(PlayerPedId())
    end)
end)